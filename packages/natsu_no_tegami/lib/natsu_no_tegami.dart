/// 夏の手紙 设计系统 —— **占位空壳，尚无实现**。
///
/// 真实的令牌与组件在上游独立仓库开发中：
/// `D:\CodeRepository\Flutter\natsu_no_tegami`
///
/// 成形后按 [COPY_IN.md] 整体拷入本包，届时这个文件会被上游的 barrel 替换，
/// 导出 `src/tokens/natsu_tokens.dart` 与 `src/components/components.dart`。
///
/// 在那之前，App 用 `apps/app/lib/app/theme.dart` 里的临时主题
/// （`KazeTempTheme`，色值抄自 docs/natsu-tokens-v2.json）开发界面。
///
/// **本包禁止在 monorepo 内手改** —— 一切改动在上游做，再跑 `make sync-ds`。
library;

// 拷入后此处将变为：
// export 'src/components/components.dart';
// export 'src/tokens/natsu_tokens.dart';
